/**
 * @param {number[]} nums
 * @return {number[][]}
 */

// [1,2,3]  ---> []

var subsets = function(nums) {
    const defaultArr = [
        []
    ];
    if (nums.length === 1) {
        defaultArr.push(nums[1]);
        return defaultArr;
    } else {

    }


};
/**
 * LeetCode -> 15
 * 
 * a + b =-c
 * 以一個base為基準加 - ，再找剩下的值，兩個加起來 = 基準點加 - 的
 * 
 * 用三個迴圈
 * a + b = -c
 * 1. 最外層找基準的number
 * 2. 把基準值加負號 變成target
 * 3. 把target當作2 sum來解
 * 
 */

// ”排除重複“會需要思考

 // indexOf 只return true, false

 /**
  * sort過後
  * 
  */

  /**
   * 內建的基本上都是最快的
   * 1. sort
   * 2. for i = 0 ~i
   *    // i === i -1 continue
   *    // min max 跳過重複的
   *    // 找到和 =0 就 push
   * 3. 
   * 
   * 
   */